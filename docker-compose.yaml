version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/dev.Dockerfile

    # Mount both repositories into the container
    volumes:
      # trading-runtime (this repo) into /workspace/trading-runtime
      - .:/workspace/trading-runtime

    # Keep the container running for the devcontainer
    command: sleep infinity
